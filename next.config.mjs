/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Configure static export mode
  images: {
    domains: ['assets.aceternity.com'],
  },
};

export default nextConfig;
